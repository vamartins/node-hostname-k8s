# Environment Variables for Local Development
# Copy this file to .env and fill in your values
# NEVER commit .env to Git!

# ======================================
# Azure Credentials (Service Principal)
# ======================================
# Get these from: az ad sp create-for-rbac --role Contributor
export ARM_CLIENT_ID="your-service-principal-client-id"
export ARM_CLIENT_SECRET="your-service-principal-secret"
export ARM_SUBSCRIPTION_ID="your-azure-subscription-id"
export ARM_TENANT_ID="your-azure-tenant-id"

# ======================================
# Docker Hub Credentials
# ======================================
# Create token at: https://hub.docker.com/settings/security
export DOCKER_USERNAME="your-docker-username"
export DOCKER_PASSWORD="your-docker-access-token"

# ======================================
# Optional: Terraform Backend
# ======================================
# If using remote state storage
# export TF_BACKEND_RESOURCE_GROUP="terraform-state-rg"
# export TF_BACKEND_STORAGE_ACCOUNT="tfstate123456"
# export TF_BACKEND_CONTAINER_NAME="tfstate"

# ======================================
# Usage Instructions
# ======================================
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual values in .env
# 3. Load before using Terraform: source .env
# 4. Run Terraform: cd terraform/azure && terraform plan
